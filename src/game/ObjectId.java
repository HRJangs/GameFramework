package game;
/*
 * 자바에서 지원되는 객체중 상수만을 모아놓은 집합을 가리켜
 * enum이라 객체라 부른다.
 * enum객체는 상수를 모아놓기 때문에 이상수값들 사이에
 * index가 존재하며 배열처럼 사용할 수 있다.
 * */


public enum ObjectId {
	Player,Enemy, Block, Bullet, Item
}
